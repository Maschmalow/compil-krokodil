int square(int x) {
  return x*x;
}
int add(int x,int y) {
  return x+y;
}
int main() {
  int A[1000];
  int i,x;
  int (*tab)(int , char);
  
  (*tab)(1,2);
  tab(1,2);
  
  for (i=0; i<1000; i++) A[i]=i;
  return x;
}
